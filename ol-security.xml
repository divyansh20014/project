<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- Define a minimal SSL context parameters bean -->
    <bean id="sslContextParameters" class="org.apache.camel.support.jsse.SSLContextParameters">
        <property name="keyManagers">
            <bean class="org.apache.camel.support.jsse.KeyManagersParameters">
                <property name="keyPassword" value="{{keyPassword}}"/>
                <property name="keyStore">
                    <bean class="org.apache.camel.support.jsse.KeyStoreParameters">
                        <property name="resource" value="{{serverKeystore}}"/>
                        <property name="password" value="{{keystorePassword}}"/>
                    </bean>
                </property>
            </bean>
        </property>
        <property name="trustManagers">
            <bean class="org.apache.camel.support.jsse.TrustManagersParameters">
                <property name="keyStore">
                    <bean class="org.apache.camel.support.jsse.KeyStoreParameters">
                        <property name="resource" value="{{serverTruststore}}"/>
                        <property name="password" value="{{truststorePassword}}"/>
                    </bean>
                </property>
            </bean>
        </property>
        <property name="serverParameters">
            <bean class="org.apache.camel.support.jsse.ServerParameters">
                <property name="clientAuthentication" value="REQUIRE"/>
            </bean>
        </property>
    </bean>

</beans>



<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:camel="http://camel.apache.org/schema/spring"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://camel.apache.org/schema/spring 
           http://camel.apache.org/schema/spring/camel-spring.xsd">

    <!-- SSL Context Parameters -->
    <camel:sslContextParameters id="sslContextParameters">
        <camel:keyManagers keyPassword="{{keyPassword}}">
            <camel:keyStore resource="{{serverKeystore}}" password="{{keystorePassword}}" />
        </camel:keyManagers>
        <camel:trustManagers>
            <camel:keyStore resource="{{serverTruststore}}" password="{{truststorePassword}}" />
        </camel:trustManagers>
        <camel:serverParameters>
            <camel:clientAuthentication>REQUIRE</camel:clientAuthentication>
        </camel:serverParameters>
    </camel:sslContextParameters>

</beans>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:camel="http://camel.apache.org/schema/spring"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://camel.apache.org/schema/spring 
           http://camel.apache.org/schema/spring/camel-spring.xsd">

    <!-- Define a simple test Camel route -->
    <camel:camelContext id="testCamelContext">
        <camel:route>
            <camel:from uri="timer:test?period=1000"/>
            <camel:log message="Test route running every second"/>
        </camel:route>
    </camel:camelContext>

</beans>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- Define a simple bean that can be referenced in Camel routes -->
    <bean id="testBean" class="org.apache.commons.lang3.time.StopWatch">
        <!-- This bean can be used or invoked in your main Camel context if needed -->
    </bean>

</beans>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:camel="http://camel.apache.org/schema/spring"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://camel.apache.org/schema/spring 
           http://camel.apache.org/schema/spring/camel-spring.xsd">

    <!-- Define a simple bean that can be referenced in Camel routes -->
    <bean id="testBean" class="com.example.TestBean"/>

    <!-- Define a Camel route that logs a message -->
    <camel:route id="logRoute">
        <camel:from uri="timer:logTimer?period=5000"/>
        <camel:log message="This is a test log message from the Camel route!"/>
    </camel:route>

</beans>
